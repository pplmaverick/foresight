# "mainnet" / "mainnet-beta" (default), "testnet", or "devnet".
NEXT_PUBLIC_CLUSTER=mainnet-beta

# Optional: override the public cluster RPC. Public devnet/mainnet-beta
# endpoints are rate-limited, so set this for anything beyond local testing.
NEXT_PUBLIC_RPC_ENDPOINT=
